<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".PHP"
    android:background="@drawable/tealabs">

    <ImageView
        android:id="@+id/phinmalogo"
        android:layout_marginTop="5dp"
        android:layout_width="210dp"
        android:layout_height="90dp"
        app:srcCompat="@drawable/phinma" />

    <ImageView
        android:id="@+id/upanglogo"
        android:layout_width="75dp"
        android:layout_height="75dp"
        android:layout_alignParentRight="true"
        android:layout_marginRight="5dp"
        android:layout_marginTop="10dp"
        app:srcCompat="@drawable/upang" />
    <ImageView
        android:id="@+id/citelogo"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_toLeftOf="@id/upanglogo"
        android:layout_marginTop="10dp"
        app:srcCompat="@drawable/cite" />

    <TextView
        android:id="@+id/jstxt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="PHP Basics"
        android:textSize="25sp"
        android:layout_marginTop="90dp"
        android:layout_marginBottom="10dp"
        android:layout_centerHorizontal="true"
        android:fontFamily="sans-serif-black"
        />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/jstxt"></androidx.recyclerview.widget.RecyclerView>

</RelativeLayout>